doctype html
html
  head
    title NodeTXT
  body
    h1 TXT2HTML
    form(action=`/upload`, method='post',enctype='multipart/form-data') 
      input(type='file', name='textfile', required=true,accept=".txt")
      input(type="submit", value="Convert!")